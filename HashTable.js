//hash table in JavaScript
//get hash function from Dan

var Hash = function (data) {
    //make arbitrary number of 'buckets'
    //implement 'hash' function to 'randomly' assign data to a storage bucket
};

Hash.prototype.insert  = function (data) {
    //function to add new data to hash function
};